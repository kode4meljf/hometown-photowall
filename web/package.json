{
  "name": "web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview",
    "deploy:adminApi": "npx tcb fn deploy adminApi --dir ../miniprogram/cloudfunctions/adminApi -e hometown-photos-d4fm4k2ca299019d --force --yes --path /adminApi",
    "deploy:fn": "bash scripts/deploy-fn.sh",
    "deploy:fns": "bash scripts/deploy-fns.sh",
    "deploy:hosting": "npm run build && npx tcb hosting deploy dist -e hometown-photos-d4fm4k2ca299019d",
    "deploy:admin": "npm run deploy:adminApi && npm run deploy:hosting"
  },
  "dependencies": {
    "vue": "^3.5.30"
  },
  "devDependencies": {
    "@cloudbase/cli": "^2.6.16",
    "@types/node": "^24.12.0",
    "@vitejs/plugin-vue": "^6.0.5",
    "@vue/tsconfig": "^0.9.0",
    "typescript": "~5.9.3",
    "vite": "^8.0.1",
    "vue-tsc": "^3.2.5"
  }
}
